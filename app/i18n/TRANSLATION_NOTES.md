# Translation Notes

- The app loads translations from local JSON files under `app/i18n/`.
- Question translations live in `app/i18n/questions/*.json`.
- UI translations live in `app/i18n/ui/*.json`.
- No runtime API calls are used. All translations are stored locally.

If you add or edit questions, regenerate the translation JSON files before deploying.
